{
  "name": "titleforge-desktop",
  "version": "0.5.0",
  "description": "TitleForge Desktop — Offline title generator for Windows, Mac, and Linux",
  "private": true,
  "scripts": {
    "dev": "tauri dev",
    "build": "tauri build"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.0.0"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.0.0"
  }
}
